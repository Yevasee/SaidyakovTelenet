<Page x:Class="SaidyakovTelenet.PageSubscribers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SaidyakovTelenet"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageSubscribers" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Grid.Row="0">
            <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Margin="10">
                <TextBlock Text="Поиск" FontSize="15" VerticalAlignment="Top" Margin="5"/>
                <TextBox Name="TextBoxSearch" TextChanged="TextBoxSearch_TextChanged" Width="200" FontSize="15" Margin="5"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Фильтр по статусу" FontSize="15" Margin="5"/>
                <ComboBox Name="ComboBoxFilter" SelectionChanged="ComboBoxFilter_SelectionChanged" FontSize="15" Width="150" Background="Transparent" Margin="5">
                    <TextBlock>Все</TextBlock>
                    <TextBlock>Активен</TextBlock>
                    <TextBlock>Приостановлен</TextBlock>
                    <TextBlock>Заблокирован</TextBlock>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Bottom">
                <ComboBox Name="ComboBoxSorter" SelectionChanged="ComboBoxSorter_SelectionChanged" FontSize="15" Width="200" Margin="5">
                    <TextBlock>Без сортировки</TextBlock>
                    <TextBlock>От А до Я</TextBlock>
                    <TextBlock>От Я до А</TextBlock>
                </ComboBox>
            </StackPanel>
            <Button Content="Добавить" Name="AddButton" Click="AddButton_Click" Padding="5" Margin="15"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        </StackPanel>


        <ListView Name="listViewSubscribers" Grid.Row="1" HorizontalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Center">
                        <Grid Background="{Binding DiscountColor}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="500"></ColumnDefinition>
                                <ColumnDefinition Width="150"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <!--<Border BorderBrush="Black" BorderThickness="1" Grid.Column="0" Margin="20, 10, 10, 10">
                                <Image HorizontalAlignment="Center">
                                    <Image.Source>
                                        <Binding Path="ProductPhoto">
                                            <Binding.TargetNullValue>
                                                <ImageSource>
                                                    /img/Icon.png
                                                </ImageSource>
                                            </Binding.TargetNullValue>
                                        </Binding>
                                    </Image.Source>
                                </Image>
                            </Border>-->

                            <Border BorderBrush="Black" BorderThickness="1" Grid.Column="0" Margin="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding SUBSCRIBER_ID, StringFormat = ID: {0}}" FontSize="20"></TextBlock>
                                        <TextBlock Text=" | " FontSize="20"></TextBlock>
                                        <TextBlock Text="{Binding subscriberFullName}" FontSize="20"></TextBlock>
                                    </StackPanel>

                                    <TextBlock Text="{Binding SUBSCRIBER_ADDRESS, StringFormat = Адрес: {0}}" FontSize="20" Grid.Row="1"/>
                                    <TextBlock Text="{Binding SUBSCRIBER_CONNECTION_DATE, StringFormat = Дата подключения: {0}}" FontSize="20" Grid.Row="2"/>
                                    <TextBlock Text="{Binding SUBSCRIBER_STATUS, StringFormat = Статус: {0}}"  FontSize="20" Grid.Row="3"/>

                                    <!--<StackPanel Name="StackPanelProductCosts" Grid.Row="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="Цена: " FontSize="20" Grid.Column="0" Grid.Row="0"/>
                                            <TextBlock Text="{Binding ProductCost}" Grid.Column="1" Grid.Row="0" FontSize="20" Margin="0,0,10,0" Foreground="{Binding CostColorForOrinal}"/>
                                            <TextBlock Text="{Binding ProductCost}" Grid.Column="1" Grid.Row="0" FontSize="20" Margin="0,0,10,0" TextDecorations="Strikethrough" Foreground="{Binding CostColorForCopy}"/>
                                            <TextBlock Text="{Binding ProductCostWithDiscount}" Grid.Column="2" Grid.Row="0" FontSize="20"/>
                                        </Grid>
                                    </StackPanel>

                                    <TextBlock Text="{Binding ProductUnit, StringFormat= Единица измерения: {0}}" FontSize="20" Grid.Row="5"/>
                                    <TextBlock Name="TextBlockProductQuantityInStock" Text="{Binding ProductQuantityInStock, StringFormat= Количество на складе: {0}}" FontSize="20" Grid.Row="6"/>-->
                                </Grid>
                            </Border>

                            <!--<Border BorderBrush="Black" BorderThickness="1" Grid.Column="2" Margin="0, 10, 10, 10">
                                <TextBlock Text="{Binding ProductDiscountAmount, StringFormat= Скидка: {0}%}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>-->
                            <Button Grid.Column="1" Grid.RowSpan="7" Name="ChangeBtn" Content="Изменить" Margin="10" Padding="10"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Click="ChangeBtn_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="10">
            <TextBlock Text="Записей: "></TextBlock>
            <TextBlock Name ="TextBlockCountOfRecordsOnPage"></TextBlock>
            <TextBlock Text=" из "></TextBlock>
            <TextBlock Name ="TextBlockCountOfRecordsInTotal"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
