<Page x:Class="SaidyakovTelenet.PageSubscriberServiceRequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SaidyakovTelenet"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageSubscriberServiceRequest">

    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="{Binding SUBSCRIBER_ID, StringFormat = ID: {0}}" FontSize="20"></TextBlock>
            <TextBlock Text=" | " FontSize="20"></TextBlock>
            <TextBlock Text="{Binding subscriberFullName}" FontSize="20"></TextBlock>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"></ColumnDefinition>
                    <ColumnDefinition Width="250"></ColumnDefinition>
                    <ColumnDefinition Width="200"></ColumnDefinition>
                    <ColumnDefinition Width="500"></ColumnDefinition>
                    <ColumnDefinition Width="150"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Text="ИД заявки" FontSize="20" Grid.Column="0"/>
                <TextBlock Text="Тип заявки" FontSize="20" Grid.Column="1"/>
                <TextBlock Text="Статус заявки" FontSize="20" Grid.Column="2"/>
                <TextBlock Text="Описание заявки" FontSize="20" Grid.Column="3"/>
                <TextBlock Text="Дата заявки" FontSize="20" Grid.Column="4"/>
            </Grid>
        </Border>

        <ListView Name="listViewServiceRequests" Grid.Row="2" HorizontalAlignment="Center" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Center">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"></ColumnDefinition>
                                <ColumnDefinition Width="250"></ColumnDefinition>
                                <ColumnDefinition Width="200"></ColumnDefinition>
                                <ColumnDefinition Width="500"></ColumnDefinition>
                                <ColumnDefinition Width="150"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding REQUEST_ID}" FontSize="20" Grid.Column="0"/>
                            <TextBlock Text="{Binding _REQUEST_TYPE_NAME}" FontSize="20" Grid.Column="1"/>
                            <TextBlock Text="{Binding _REQUEST_STATUS_NAME}" FontSize="20" Grid.Column="2"/>
                            <TextBlock Text="{Binding REQUEST_DESCRIPTION}" FontSize="20" Grid.Column="3"/>
                            <TextBlock Text="{Binding _REQUEST_CREATION_DATE}" FontSize="20" Grid.Column="4"/>

                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
